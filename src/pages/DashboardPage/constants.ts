// === Pagination ===
export const PAGE_SIZE = 10;
export const NEW_TIER_LIST_THRESHOLD_MS = 24 * 60 * 60 * 1000; // 24 часа

// === Date format ===
export const DATE_FORMAT: Intl.DateTimeFormatOptions = {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
};

// === Tier List Limits ===
export const MAX_BOOKS_PER_TIER_LIST = 20;
