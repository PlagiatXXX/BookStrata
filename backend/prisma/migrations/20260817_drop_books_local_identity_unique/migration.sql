-- Решение 17.08: возврат к per-user книгам в тир-листах.
-- Локальные книги (source IS NULL) больше НЕ дедуплицируются по (title, authorId):
-- каждое добавление создаёт пользователю свой оригинал (поведение до b98efc8).
-- Индекс снимаем целиком: title-матч при добавлении удалён из кода
-- (findExistingUserBook), использовать его больше нечем.
DROP INDEX books_local_identity_idx;
